const TelegramApi = require('node-telegram-bot-api')
const token = process.env.TOKEN

class BotService {


    // async sendMSG(data) {
    //     let msg = `${data.firstName + '' + data.secondName} отработал ${data.hours} \n
    //     *${data.project.name} --- ${data.project.hours} ч.
    //     `
    //     const bot = new TelegramApi(token, {polling: true})
    //     bot.sendMessage()
    // }

    //как-то нужно вытащить часы разрабочтиков
    //async sendStatistics()
}